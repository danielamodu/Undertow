__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
build/

.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Undertow's local baseline store. Snapshots are reproducible from DataHub,
# so they are cache, not source.
.undertow/
undertow-report.json

.env
.DS_Store
.idea/
.vscode/

# Working notes for the submission itself — the recording script, the Devpost
# draft. They are about presenting the project, not about the project, and a
# public repository should carry the second kind only.
_submission/
